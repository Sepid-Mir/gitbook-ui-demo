# gitbook-ui-demo
This repository is a small example to test how GitBook handles:

- Markdown and MDX files
- React components imported from `.jsx` files
- Live UI examples inside documentation pages

The main docs live in the `docs/` folder.
